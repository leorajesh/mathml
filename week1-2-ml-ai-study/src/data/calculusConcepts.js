// Vector calculus pages, following Chapter 5 of the course reference book, "Mathematics for Machine
// Learning" (Deisenroth, Faisal, Ong): derivatives and Taylor polynomials (§5.1), partial derivatives
// and gradients (§5.2), Jacobians and the chain rule (§5.2-5.3), useful gradient identities and the
// least-squares gradient (§5.3-5.5), backpropagation (§5.6), and the Hessian and multivariate Taylor
// series (§5.7-5.8). The explanations, numbers, and pictures are our own; the book is cited on each page.

const tex = String.raw;

export const calculusConcepts = [
  {
    id: 'derivatives',
    title: 'Derivatives and the Chain Rule',
    group: 'Optimization',
    week: 'MML book §5.1',
    problem: 'It measures how fast a function\'s output changes when its input changes, which is exactly what an optimizer needs to know to decide which way to move.',
    intuition: 'Zoom in far enough on a smooth curve and it looks like a straight line; the derivative is the slope of that line. To find it, take the slope of a short chord from x to x + h (the difference quotient) and let h shrink toward 0. A positive derivative means the function rises as x grows, a negative one means it falls, and a zero derivative marks a flat spot, such as the bottom of a valley. You rarely need the limit itself: a few rules (sum, product, quotient, and the chain rule for a function inside a function) differentiate almost anything built from simple pieces. The chain rule matters most in machine learning: the rate of change of g(f(x)) is the rate of g at f(x) times the rate of f at x.',
    formulas: [
      { tex: tex`f'(x)=\lim_{h\to0}\frac{f(x+h)-f(x)}{h}`, definitions: [tex`f'(x): derivative, the slope of the tangent line at x`, tex`h: a small step; the fraction is the difference quotient, the slope of a chord`] },
      { tex: tex`(f+g)'=f'+g',\qquad (fg)'=f'g+fg',\qquad \left(\frac{f}{g}\right)'=\frac{f'g-fg'}{g^2}`, definitions: [tex`f,g: differentiable functions (g not zero in the quotient rule)`] },
      { tex: tex`(g\circ f)'(x)=g'(f(x))\,f'(x)`, definitions: [tex`g\circ f: apply f first, then g (chain rule)`] },
      { tex: tex`(x^n)'=nx^{n-1},\qquad (e^x)'=e^x,\qquad (\log x)'=\frac1x,\qquad \sigma'(s)=\sigma(s)\bigl(1-\sigma(s)\bigr)`, definitions: [tex`\log: natural logarithm`, tex`\sigma: the sigmoid 1/(1 + e^(-s)) used by logistic regression`] },
    ],
    example: ['Differentiate f(x) = (2x + 1)^3 at x = 1 with the chain rule: the outer function is g(u) = u^3 and the inner one is u = 2x + 1.', 'g\'(u) = 3u^2 and du/dx = 2, so f\'(x) = 3(2x + 1)^2 * 2 = 6(2x + 1)^2, and f\'(1) = 6 * 9 = 54.', 'Check with a difference quotient, h = 0.001: (f(1.001) - f(1))/0.001 = (3.002^3 - 27)/0.001 ≈ 54.036, close to 54.', 'The sigmoid sigma(s) = 1/(1 + e^(-s)) has derivative e^(-s)/(1 + e^(-s))^2 = sigma(s)(1 - sigma(s)). At s = 0, sigma = 0.5 and the slope is 0.25.'],
    graph: { type: 'tangentLine', title: 'From chord to tangent', caption: 'The dark curve is f(x) = x^3/4 - x. The dashed chord joins x0 and x0 + h; its slope is the difference quotient. As h shrinks, the chord turns into the tangent line (red), whose slope is the derivative.', sliders: [{ key: 'x0', label: 'point x0', min: -2.5, max: 2.5, step: 0.1, value: 1 }, { key: 'h', label: 'step h', min: -2, max: 2, step: 0.05, value: 1.5 }] },
    misconception: 'A zero derivative does not guarantee a minimum: f(x) = x^3 has slope 0 at x = 0 but keeps rising through it, and a maximum has slope 0 too.',
    prerequisites: ['functions', 'inverse-composition'],
    followOns: ['partial-derivatives-gradient', 'taylor-hessian'],
    sources: ['Mathematics for Machine Learning, §5.1'],
  },
  {
    id: 'partial-derivatives-gradient',
    title: 'Partial Derivatives and the Gradient',
    group: 'Optimization',
    week: 'MML book §5.2',
    problem: 'It extends the derivative to functions of many inputs. The result, the gradient, is the direction of steepest increase that gradient descent steps against.',
    intuition: 'With several inputs, change one at a time: hold the others fixed and take the ordinary derivative. That is a partial derivative, the slope along one coordinate axis. Collect all of them and you have the gradient. It points in the direction in which f increases fastest, its length is that fastest rate, and it is perpendicular to the level curve (contour) through the point. So walking against the gradient is the steepest way down, which is the whole idea of gradient descent. The slope in any other unit direction u is the dot product of the gradient with u. The MML book writes the gradient as a row vector, which makes the chain rule a plain matrix product; update steps use the matching column vector.',
    formulas: [
      { tex: tex`\frac{\partial f}{\partial x_i}=\lim_{h\to0}\frac{f(x+h\,e_i)-f(x)}{h}`, definitions: [tex`e_i: the i-th unit vector, so only coordinate i moves`] },
      { tex: tex`\nabla f(x)=\frac{df}{dx}=\left[\frac{\partial f}{\partial x_1}\ \cdots\ \frac{\partial f}{\partial x_n}\right]\in\mathbb{R}^{1\times n}`, definitions: [tex`\nabla f: gradient; the MML book writes it as a row vector, and update steps use its transpose`] },
      { tex: tex`D_uf(x)=\nabla f(x)\,u,\qquad \max_{\lVert u\rVert=1}D_uf(x)=\lVert\nabla f(x)\rVert`, definitions: [tex`u: a unit direction (a column vector)`, tex`D_uf: directional derivative, the slope of f in direction u`] },
    ],
    example: ['Let f(x, y) = x^2 y + 3y and look at the point (1, 2), where f = 2 + 6 = 8.', 'Holding y fixed: df/dx = 2xy = 4. Holding x fixed: df/dy = x^2 + 3 = 4. So the gradient is [4, 4].', 'The steepest climb is along [1, 1]/sqrt(2), with slope ||grad f|| = sqrt(32) ≈ 5.66. Along [1, 0] the slope is only 4, and along [1, -1]/sqrt(2) it is 0: that direction runs along the level curve.', 'Check numerically with h = 0.001: (f(1.001, 2) - f(1, 2))/0.001 = (8.004002 - 8)/0.001 ≈ 4.002.'],
    graph: { type: 'gradientField', title: 'The gradient is perpendicular to the contour', caption: 'Contours of f(x, y) = x^2/2 + y^2. The red arrow is the gradient at the point (drawn at half length); the blue arrow is a unit direction u (drawn 1.5 times longer). The slope along u is largest when u lines up with the gradient and zero along the contour.', sliders: [{ key: 'px', label: 'point x', min: -4, max: 4, step: 0.25, value: 2 }, { key: 'py', label: 'point y', min: -3, max: 3, step: 0.25, value: 1 }, { key: 'angle', label: 'direction u (degrees)', min: 0, max: 360, step: 5, value: 90 }] },
    misconception: 'The gradient does not point along the contour or toward the minimum. It is perpendicular to the contour and points uphill; it points straight at the minimum only in special cases, such as circular contours.',
    prerequisites: ['derivatives', 'vectors-dot-product'],
    followOns: ['jacobian-chain-rule', 'loss-gradients', 'gradient-descent-method'],
    sources: ['Mathematics for Machine Learning, §5.2'],
  },
  {
    id: 'jacobian-chain-rule',
    title: 'The Jacobian and the Multivariate Chain Rule',
    group: 'Optimization',
    week: 'MML book §5.2.2-5.3',
    problem: 'It describes how a function with several outputs responds to small changes in its inputs, and it passes gradients through chains of functions, as in neural networks.',
    intuition: 'For a function f from R^n to R^m, each output has its own gradient. Stack those rows and you get the m by n Jacobian matrix. Near a point, f behaves like the linear map given by its Jacobian: a tiny input step dx moves the output by about J dx. For a linear function f(x) = Ax the Jacobian is just A. For a map from R^2 to R^2, the absolute value of the Jacobian\'s determinant says how much a tiny square\'s area is stretched, as on the determinant page. The chain rule becomes matrix multiplication: the Jacobian of g after f is the Jacobian of g (taken at f(x)) times the Jacobian of f.',
    formulas: [
      { tex: tex`J=\frac{\partial f}{\partial x}=\begin{bmatrix}\frac{\partial f_1}{\partial x_1}&\cdots&\frac{\partial f_1}{\partial x_n}\\ \vdots&&\vdots\\ \frac{\partial f_m}{\partial x_1}&\cdots&\frac{\partial f_m}{\partial x_n}\end{bmatrix}\in\mathbb{R}^{m\times n}`, definitions: [tex`f_i: the i-th output of f`, tex`J: Jacobian; row i is the gradient of f_i`] },
      { tex: tex`f(x+dx)\approx f(x)+J\,dx,\qquad f(x)=Ax\ \Rightarrow\ J=A`, definitions: [tex`dx: a small change of the input`] },
      { tex: tex`\frac{\partial (g\circ f)}{\partial x}=\frac{\partial g}{\partial f}\,\frac{\partial f}{\partial x}`, definitions: [tex`\frac{\partial g}{\partial f}: Jacobian of g, evaluated at f(x)`] },
    ],
    example: ['Map polar coordinates to the plane: f(r, theta) = [r cos theta, r sin theta].', 'Its Jacobian is J = [[cos theta, -r sin theta], [sin theta, r cos theta]], a 2 by 2 matrix (2 outputs, 2 inputs), with determinant r cos^2 theta + r sin^2 theta = r.', 'At r = 2, theta = 0: J = [[1, 0], [0, 2]]. A step dr = 0.01 moves the point by about J[0.01, 0] = [0.01, 0], but a step dtheta = 0.01 moves it by about [0, 0.02]: the same angle covers more ground far from the origin. Small areas grow by |det J| = 2.', 'Chain rule: g(x, y) = x^2 + y^2 has gradient [2x, 2y] = [4, 0] at (2, 0). The gradient of g(f(r, theta)) is [4, 0] J = [4, 0], so d/dr = 4 and d/dtheta = 0. Check directly: g(f(r, theta)) = r^2, whose derivatives are 2r = 4 and 0.'],
    graph: { type: 'jacobianMap', title: 'Near a point, a curved map is almost linear', caption: 'The map f(x, y) = (x + 0.2y^2, y + 0.2x^2) bends the small grey square into the teal shape. The dashed parallelogram is the Jacobian\'s linear approximation. Shrink the square and the two match; the area ratio approaches |det J|.', sliders: [{ key: 'px', label: 'point x', min: -2, max: 2, step: 0.25, value: 1.5 }, { key: 'py', label: 'point y', min: -2, max: 2, step: 0.25, value: 1 }, { key: 'size', label: 'square size', min: 0.2, max: 1.6, step: 0.1, value: 1.2 }] },
    misconception: 'The Jacobian is not a single number. For m outputs and n inputs it is an m by n matrix, and the order of the factors in the chain rule matters because matrix products do not commute.',
    prerequisites: ['partial-derivatives-gradient', 'transformation-matrix', 'determinant-geometry'],
    followOns: ['backpropagation', 'loss-gradients'],
    sources: ['Mathematics for Machine Learning, §5.2.2-5.3'],
  },
  {
    id: 'loss-gradients',
    title: 'Gradients of Vector Expressions and Loss Functions',
    group: 'Optimization',
    week: 'MML book §5.3-5.5',
    problem: 'It gives the handful of vector derivative rules needed to compute the gradients of common machine learning losses, such as least squares and logistic loss, by hand.',
    intuition: 'Differentiating a loss one coordinate at a time works but gets messy. A few identities do the same job in one line, just as x^2 differentiates to 2x: the gradient of a linear function a^T x is a^T, and the gradient of a quadratic x^T A x is x^T (A + A^T). With the chain rule they give the gradient of the least-squares loss ||y - X theta||^2, and setting it to zero gives the normal equation. The same steps give the gradient of logistic loss, which has a memorable shape: prediction error times input, averaged over the data. A numerical check (one difference quotient per coordinate) catches most algebra slips.',
    formulas: [
      { tex: tex`\frac{\partial\,a^Tx}{\partial x}=a^T,\qquad \frac{\partial\,x^TAx}{\partial x}=x^T\left(A+A^T\right)`, definitions: [tex`a: a constant vector`, tex`A: a constant square matrix; when A is symmetric the gradient is 2 x^T A`] },
      { tex: tex`L(\theta)=\lVert y-X\theta\rVert^2\ \Rightarrow\ \frac{dL}{d\theta}=-2\,(y-X\theta)^TX`, definitions: [tex`X: data matrix, one example per row`, tex`y-X\theta: residual vector`] },
      { tex: tex`\frac{dL}{d\theta}=0\iff X^TX\theta=X^Ty`, definitions: [tex`X^TX\theta=X^Ty: the normal equation of least squares`] },
      { tex: tex`J(\theta)=\frac1n\sum_{t=1}^{n}L_{\log}\bigl(y^{(t)},\sigma(\theta\cdot x^{(t)})\bigr)\ \Rightarrow\ \nabla J(\theta)=\frac1n\sum_{t=1}^{n}\bigl(\sigma(\theta\cdot x^{(t)})-y^{(t)}\bigr)\,x^{(t)}`, definitions: [tex`L_{\log}: logistic (cross-entropy) loss with labels y in {0, 1}`, tex`\sigma(\theta\cdot x^{(t)})-y^{(t)}: prediction error on example t; a constant feature 1 in x takes the place of the offset theta_0`, tex`\nabla J(\theta): written as a column (the transpose of the row gradient) so it can be used directly in theta <- theta - alpha grad J`] },
    ],
    example: ['Least squares through the origin on the points (1, 2) and (2, 3): L(theta) = (2 - theta)^2 + (3 - 2theta)^2, which is ||y - X theta||^2 with X = [1, 2]^T and y = [2, 3]^T.', 'Gradient: dL/dtheta = -2(y - X theta)^T X = -2[(2 - theta)*1 + (3 - 2theta)*2] = -2(8 - 5theta). At theta = 0 it is -16, so increasing theta lowers the loss.', 'Setting it to zero gives 5 theta = 8, so theta = 1.6: the normal equation X^T X theta = X^T y with X^T X = 5 and X^T y = 8.', 'Logistic loss for one example x = [1, 2] (the leading 1 plays the role of the offset), label y = 1, starting at theta = [0, 0]: sigma(0) = 0.5, so the gradient is (0.5 - 1)[1, 2] = [-0.5, -1].', 'A gradient step with alpha = 0.5 gives theta = [0.25, 0.5]. The new score is 0.25 + 1 = 1.25 and sigma(1.25) ≈ 0.777: the predicted probability of the true class rose from 0.5.'],
    graph: { type: 'lossSurface', title: 'Gradient descent on a least-squares loss', caption: 'Contours of the squared error of the line y = a x + b on the points (1, 1), (2, 2), (3, 2), over the slope a and intercept b. The red arrow is the negative gradient (scaled down); the orange path takes gradient steps with step size 0.03. The minimum is a = 0.5, b = 0.67.', sliders: [{ key: 'a', label: 'start slope a', min: -1, max: 2, step: 0.05, value: 1.6 }, { key: 'b', label: 'start intercept b', min: -1.5, max: 3, step: 0.05, value: -0.8 }, { key: 'steps', label: 'gradient steps', min: 0, max: 80, step: 1, value: 40 }] },
    misconception: 'The gradient of x^T A x is not always 2 x^T A; that shortcut needs A to be symmetric. In general it is x^T (A + A^T).',
    prerequisites: ['partial-derivatives-gradient', 'jacobian-chain-rule', 'matrix-multiplication-outer-product'],
    followOns: ['least-squares-normal-equation', 'logistic-loss', 'gradient-descent-method'],
    sources: ['Mathematics for Machine Learning, §5.3-5.5'],
  },
  {
    id: 'backpropagation',
    title: 'Backpropagation and Automatic Differentiation',
    group: 'Optimization',
    week: 'MML book §5.6',
    problem: 'It computes the gradient of a long chain of functions, such as a neural network\'s loss, exactly and efficiently, by reusing the intermediate results.',
    intuition: 'Write the computation as a graph of small steps, each with a simple derivative. The forward pass computes and stores every intermediate value. The backward pass starts at the output, whose derivative with respect to itself is 1, and walks the graph in reverse: each node multiplies the derivative arriving from later steps by its own local derivative (the chain rule) and passes the result to its inputs. Where a value feeds several later steps, the contributions add up. One backward pass gives the derivative with respect to every input for about the cost of a forward pass or two, which is what makes training networks with millions of weights possible. Software does this automatically (automatic differentiation): the result is exact up to rounding, unlike finite differences.',
    formulas: [
      { tex: tex`v_i=g_i(\text{parents of }v_i),\qquad \frac{\partial f}{\partial v_i}=\sum_{j\,:\,v_i\to v_j}\frac{\partial f}{\partial v_j}\,\frac{\partial g_j}{\partial v_i}`, definitions: [tex`v_i: intermediate values of the computation graph, computed in the forward pass`, tex`g_j: the simple function computed at node j`, tex`\frac{\partial f}{\partial v_i}: computed in the backward pass, from the output toward the inputs`] },
      { tex: tex`z=wx+b,\quad m=yz,\quad L=\log\left(1+e^{-m}\right)`, definitions: [tex`y: label in {-1, +1}`, tex`m: signed margin; L is the logistic loss written with plus-minus labels`, tex`\tilde y: with ỹ = (y + 1)/2 in {0, 1} this is the same loss as the cross-entropy on the logistic-loss page, and dL/dz = sigma(z) - ỹ, the same prediction-error gradient`] },
      { tex: tex`\frac{\partial L}{\partial m}=-\frac{e^{-m}}{1+e^{-m}}=-\sigma(-m),\qquad \frac{\partial L}{\partial w}=\frac{\partial L}{\partial m}\,y\,x,\qquad \frac{\partial L}{\partial b}=\frac{\partial L}{\partial m}\,y`, definitions: [tex`\sigma: sigmoid function`] },
    ],
    example: ['Logistic loss for one example as a graph: z = w x + b, then m = y z, then L = log(1 + e^(-m)). Take x = 2, y = +1, w = 0.5, b = -0.5.', 'Forward pass: z = 0.5*2 - 0.5 = 0.5, m = 0.5, and L = log(1 + e^(-0.5)) = log(1.6065) ≈ 0.474.', 'Backward pass: dL/dm = -e^(-m)/(1 + e^(-m)) = -0.6065/1.6065 ≈ -0.378. Then dL/dz = dL/dm * y ≈ -0.378.', 'Finally dL/dw = dL/dz * x ≈ -0.755 and dL/db = dL/dz * 1 ≈ -0.378. Both are negative, so increasing w and b lowers the loss by pushing the margin up.'],
    graph: { type: 'computationGraph', title: 'Forward values and backward gradients', caption: 'Each box shows its forward value (top) and the derivative of the loss with respect to it (bottom, orange), for L = log(1 + e^(-y(wx + b))). Change the inputs and watch both passes update; the readout checks dL/dw against a finite difference.', sliders: [{ key: 'w', label: 'weight w', min: -2, max: 2, step: 0.1, value: 0.5 }, { key: 'b', label: 'offset b', min: -2, max: 2, step: 0.1, value: -0.5 }, { key: 'x', label: 'input x', min: -3, max: 3, step: 0.5, value: 2 }, { key: 'y', label: 'label y (-1 or +1)', min: -1, max: 1, step: 2, value: 1 }] },
    figure: 'computation-graph',
    misconception: 'Backpropagation is not a learning algorithm of its own, nor an approximation. It is the chain rule applied in a clever order, and it gives exact gradients (up to rounding); the learning is still done by gradient descent.',
    prerequisites: ['jacobian-chain-rule'],
    followOns: ['logistic-loss', 'gradient-descent-method'],
    sources: ['Mathematics for Machine Learning, §5.6'],
  },
  {
    id: 'taylor-hessian',
    title: 'Taylor Approximation and the Hessian',
    group: 'Optimization',
    week: 'MML book §5.1.1, §5.7-5.8',
    problem: 'It approximates a complicated function near a point by a simple polynomial, and uses second derivatives (the Hessian) to measure curvature, which decides whether a flat point is a minimum.',
    intuition: 'Near a point x0, a smooth function looks like its tangent line; add a curvature term and it looks like a parabola. Each extra term of the Taylor polynomial matches one more derivative at x0, so the approximation stays close over a wider range. In several dimensions, the first-order term uses the gradient and the second-order term uses the Hessian, the matrix of all second partial derivatives. For smooth functions the Hessian is symmetric, and its eigenvalues are the curvatures along its eigenvectors: all positive means a bowl (a local minimum, if the gradient is zero there), all negative a dome, mixed signs a saddle. A positive semidefinite Hessian everywhere is exactly the convexity test on the convex-functions page, and Newton\'s method jumps straight to the minimum of the quadratic approximation.',
    formulas: [
      { tex: tex`T_n(x)=\sum_{k=0}^{n}\frac{f^{(k)}(x_0)}{k!}\,(x-x_0)^k`, definitions: [tex`f^{(k)}(x_0): the k-th derivative of f at x_0`, tex`T_n: Taylor polynomial of degree n`] },
      { tex: tex`H=\nabla^2f=\left[\frac{\partial^2f}{\partial x_i\,\partial x_j}\right],\qquad f(x_0+\delta)\approx f(x_0)+\nabla f(x_0)\,\delta+\tfrac12\,\delta^TH\,\delta`, definitions: [tex`H: Hessian, a symmetric n by n matrix for smooth f`, tex`\delta: a small step away from x_0 (a column vector)`] },
      { tex: tex`\nabla f(x^*)=0\ \text{and}\ H\succ0\ \Rightarrow\ \text{local minimum};\qquad H\ \text{with eigenvalues of both signs}\ \Rightarrow\ \text{saddle}`, definitions: [tex`H\succ0: positive definite, all eigenvalues positive`] },
      { tex: tex`x_{\text{new}}=x_0-H^{-1}\,\nabla f(x_0)^T`, definitions: [tex`x_{\text{new}}: Newton step, the minimum of the quadratic approximation (when H is positive definite)`] },
    ],
    example: ['Approximate f(x) = e^x near x0 = 0. Every derivative of e^x is e^x, which is 1 at 0, so T1(x) = 1 + x and T2(x) = 1 + x + x^2/2.', 'At x = 0.5: e^0.5 ≈ 1.6487, T1 = 1.5 (error 0.149) and T2 = 1.625 (error 0.024). Each extra term cuts the error.', 'In two variables, f(x, y) = x^2 + 3xy + y^2 has gradient [2x + 3y, 3x + 2y], which is zero at the origin.', 'Its Hessian is [[2, 3], [3, 2]], with eigenvalues 5 and -1. Mixed signs make the origin a saddle point, not a minimum: along [1, -1] the function goes down, f(t, -t) = -t^2.'],
    graph: { type: 'taylor', title: 'Taylor polynomials of sin x', caption: 'The dark curve is sin x and the red curve is the Taylor polynomial of degree n around x0 (orange dot). Higher degrees match more derivatives and stay close over a wider range; far from x0 every polynomial eventually drifts away.', sliders: [{ key: 'x0', label: 'expansion point x0', min: -3, max: 3, step: 0.1, value: 0 }, { key: 'n', label: 'degree n', min: 0, max: 7, step: 1, value: 3 }] },
    misconception: 'A zero gradient is not enough for a minimum: the point can be a maximum or a saddle. The Hessian decides, and when it is only semidefinite (some eigenvalue 0) the second-order test is inconclusive.',
    prerequisites: ['derivatives', 'partial-derivatives-gradient', 'spectral-theorem'],
    followOns: ['convex-functions', 'gradient-descent-method', 'momentum'],
    sources: ['Mathematics for Machine Learning, §5.1.1 and §5.7-5.8'],
  },
];
