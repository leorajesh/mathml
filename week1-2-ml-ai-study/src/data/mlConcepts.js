// Machine learning pages added from the course reading list: Bishop (Pattern Recognition and Machine
// Learning), James et al. (An Introduction to Statistical Learning), Duda, Hart & Stork (Pattern
// Classification), Mitchell (Machine Learning), Huyen (Designing Machine Learning Systems), and
// Sculley et al. (Machine Learning: The High-Interest Credit Card of Technical Debt). They fill gaps in
// the Week 1-2 pages: the maximum-margin view of hinge loss, feature scaling and leakage, the
// bias-variance trade-off, ROC curves, and what changes once a model is in production.
// Explanations, numbers, and pictures are our own; the books are cited on each page.

const tex = String.raw;

export const mlConcepts = [
  {
    id: 'ml-landscape',
    title: 'Machine Learning and MLOps: The Big Picture',
    group: 'Problem',
    week: 'Production ML W1 (Lesson 1)',
    problem: 'It places machine learning within AI, names the main kinds of learning problems, and explains why a production course also needs MLOps: the practices that take a model from a notebook to reliable use.',
    intuition: 'Machine learning is how computers learn rules from examples instead of being told the rules. This course cares about the whole journey: not just training a model, but keeping it useful once real people depend on it.',
    formulas: [
      { tex: tex`\text{supervised: } S_n=\{(x^{(t)},y^{(t)})\}_{t=1}^{n},\ \ h:\mathcal{X}\to\mathcal{Y};\qquad \mathcal{Y}=\{-1,+1\}\ \text{(classification)},\ \ \mathcal{Y}=\mathbb{R}\ \text{(regression)}`, definitions: [tex`\mathcal{X}: input space, for example 30 real-valued measurements per tumour`, tex`\mathcal{Y}: output space; a category for classification, a number for regression`] },
      { tex: tex`\text{unsupervised: } \{x^{(t)}\}_{t=1}^{n}\ \text{only};\qquad \text{reinforcement: choose actions } a_t \text{ to maximize } \mathbb{E}\Bigl[\sum_t r_t\Bigr]`, definitions: [tex`r_t: reward received at time t`] },
      { tex: tex`\text{MLOps}=\text{tools}+\text{processes}+\text{mindset}\ \Rightarrow\ \text{a reproducible, trackable, testable, maintainable ML lifecycle}`, definitions: ['phases: data, then model, then operations, repeated as the data changes'] },
    ],
    example: ['The breast cancer case study from the Lesson 1 slides: each row of the table is one tumour (a training example or instance), described by 30 real-valued features measured from images of its cells.', 'Predicting the outcome, recurrence (R) or no recurrence (N), is binary classification: the label is a category, which can be encoded as +1 and -1.', 'Predicting the time until recurrence (or the time the patient stays disease-free) is regression: the target is a number.', 'Across the lifecycle, the data phase fixes the question and collects labelled rows, the model phase trains and validates a model, and the operations phase deploys, versions, and monitors it, checking that new patients still look like the training data.'],
    graph: { type: 'mlopsPhases', title: 'The three phases of MLOps', caption: 'Move through the phases of the ML lifecycle. Each phase lists its main activities; the loop back from operations to data is where monitoring triggers new data collection and retraining.', sliders: [{ key: 'phase', label: 'phase (1 = data, 2 = model, 3 = operations)', min: 1, max: 3, step: 1, value: 1 }] },
    figure: 'ml-lifecycle',
    misconception: 'Production ML is not ML with a deployment step bolted on at the end. Most of the work, and most of the ways a system fails, lie outside the model: data, pipelines, infrastructure, and monitoring. The slides quote the Hidden Technical Debt paper: Knight Capital lost $465 million in 45 minutes, apparently because of unexpected behaviour from obsolete experimental code paths. (That was a trading-software failure rather than an ML model failure; the paper uses it to warn about dead code paths.)',
    prerequisites: [],
    followOns: ['ml-workflow', 'feature-representation', 'linear-classifier', 'linear-regression', 'ml-in-production'],
    sources: ['Production ML slides - Introduction Lesson1.pdf', 'Week1_01-Introduction.pdf'],
  },
  {
    id: 'max-margin-svm',
    title: 'Maximum Margin and the Regularized Hinge Objective (SVM)',
    group: 'Loss',
    week: 'Beyond the notes · Bishop §7.1 · ISL §9.1-9.2',
    problem: 'Among all boundaries that separate the data, it picks the one with the widest safety gap, which tends to generalize well. Adding the penalty (lambda/2)||theta||^2 to the course\'s hinge risk R_n gives this objective; stochastic subgradient descent can minimize it too, with each step also shrinking theta (lambda = 0 is the course\'s rule).',
    intuition: 'When many lines separate the data, pick the one that stays farthest from both classes, like driving down the middle of a road. The wider the safety gap, the better it usually generalizes.',
    formulas: [
      { tex: tex`\text{dist}(x)=\frac{|\theta\cdot x+\theta_0|}{\lVert\theta\rVert}`, definitions: [tex`\text{dist}(x): distance from the point x to the boundary theta dot x + theta_0 = 0`] },
      { tex: tex`\theta\cdot x+\theta_0=\pm1,\qquad \text{margin width}=\frac{2}{\lVert\theta\rVert}`, definitions: ['margin lines: where the score is exactly +1 or -1; points on them are support vectors'] },
      { tex: tex`J(\theta,\theta_0)=\frac{\lambda}{2}\lVert\theta\rVert^2+\frac1n\sum_{t=1}^{n}\max\bigl(0,\ 1-y^{(t)}(\theta\cdot x^{(t)}+\theta_0)\bigr)`, definitions: [tex`\lambda: regularization strength; larger means a wider margin and more violations allowed`, tex`J: regularized hinge objective (the soft-margin support vector machine); it is convex`] },
      { tex: tex`\min_{\theta,\theta_0}\ \tfrac12\lVert\theta\rVert^2\quad\text{s.t.}\quad y^{(t)}(\theta\cdot x^{(t)}+\theta_0)\ge1\ \ \forall t`, definitions: ['hard margin: the separable case, where no point may be inside the margin; it is solved with Lagrange multipliers'] },
    ],
    example: ['Two points: x = [2, 2] with y = +1 and x = [0, 0] with y = -1. Try theta = [0.5, 0.5] and theta_0 = -1.', 'Scores: 0.5*2 + 0.5*2 - 1 = 1 for the positive point and -1 for the negative one. Both signed margins are exactly 1, so both points lie on the margin lines: they are the support vectors.', '||theta|| = sqrt(0.5) ≈ 0.707, so the margin width is 2/0.707 ≈ 2.83. That equals the distance between the two points, sqrt(8) ≈ 2.83: the boundary x1 + x2 = 2 sits exactly halfway.', 'A smaller theta with the same boundary, such as [0.25, 0.25] with theta_0 = -0.5, gives signed margins of only 0.5, so it pays hinge loss 0.5 per point. Requiring margins of at least 1 is what fixes the scale of theta.'],
    graph: { type: 'maxMargin', title: 'Soft-margin SVM: wide margin versus few violations', caption: 'Blue points are y = +1 and orange points y = -1. The solid line is the boundary and the dashed lines are the margin lines (score +1 and -1); ringed points are on or inside the margin. Raise lambda to widen the margin at the cost of more violations.', sliders: [{ key: 'lambda', label: 'regularization lambda', min: 0.02, max: 1.5, step: 0.02, value: 0.1 }] },
    misconception: 'A support vector machine does not draw a different kind of boundary: it is still a linear classifier sign(theta . x + theta_0). What changes is the objective used to choose theta, which rewards a wide margin instead of only counting training mistakes.',
    prerequisites: ['hinge-loss', 'linear-separability', 'norms'],
    followOns: ['convex-functions', 'stochastic-subgradient-descent', 'lagrange-multipliers', 'ridge-regularization'],
    sources: ['Week1_03-HingeLoss.pdf', 'Bishop §7.1', 'James et al. §9.1-9.2'],
  },
  {
    id: 'feature-scaling',
    title: 'Feature Scaling, Encoding, and Data Leakage',
    group: 'Representation',
    week: 'Production ML W2 · Huyen Ch. 5 · ISL §6.2.1',
    problem: 'It puts features on comparable scales and in usable formats, which makes gradient descent faster, makes penalties such as ridge and lasso fair to every feature, and keeps test data from leaking into training.',
    intuition: 'Describe a house by [bedrooms, square feet] and one number is about 3 while the other is about 2000. Penalties and distances then listen mostly to the big one. Scaling puts the features on an even footing.',
    formulas: [
      { tex: tex`z=\frac{x-\mu}{\sigma},\qquad \mu=\frac1n\sum_{t=1}^{n}x^{(t)},\qquad \sigma=\sqrt{\frac1n\sum_{t=1}^{n}\bigl(x^{(t)}-\mu\bigr)^2}`, definitions: [tex`x: a raw feature value`, tex`\mu: the feature's mean on the training set`, tex`\sigma: the feature's standard deviation on the training set`, tex`z: standardized value (mean 0, standard deviation 1 on the training set)`] },
      { tex: tex`x'=\frac{x-x_{\min}}{x_{\max}-x_{\min}}`, definitions: [tex`x': min-max scaled value, in [0, 1] for the training data`] },
      { tex: tex`X'=XD\ \Rightarrow\ X'^TX'=D\,X^TX\,D,\qquad \kappa=\frac{\lambda_{\max}(X^TX)}{\lambda_{\min}(X^TX)}`, definitions: [tex`D: diagonal matrix of per-feature scale factors`, tex`\kappa: condition number; a large kappa means a narrow valley and slow gradient descent`] },
    ],
    example: ['Training values of a feature: 1000, 1500, 2000 (square feet). The mean is mu = 1500.', 'Standard deviation: sqrt(((-500)^2 + 0^2 + 500^2)/3) = sqrt(166667) ≈ 408.2.', 'Standardized training values: -500/408.2 ≈ -1.22, 0, and 1.22.', 'A test house with 2500 square feet uses the training mu and sigma: (2500 - 1500)/408.2 ≈ 2.45. Recomputing mu and sigma with the test house included would leak test information into the model.'],
    graph: { type: 'scaling', title: 'Unscaled features make a narrow valley', caption: 'Contours of a least-squares loss over two weights when the second feature is s times larger in scale than the first, and the gradient-descent path with step size 1.8/s^2, just under the stability limit 2/s^2 (step 1 once the contours are round). Standardizing makes the contours round and gradient descent fast.', sliders: [{ key: 's', label: 'scale ratio s of feature 2', min: 1, max: 6, step: 0.5, value: 4 }, { key: 'standardize', label: 'standardize (0 = no, 1 = yes)', min: 0, max: 1, step: 1, value: 0 }, { key: 'steps', label: 'gradient steps', min: 1, max: 40, step: 1, value: 15 }] },
    misconception: 'Scaling does not change what a linear model can represent (the weights rescale, and the intercept absorbs the shift by the mean), but it changes how fast gradient descent trains, how ridge and lasso penalize each feature, and how distance-based methods behave. The standard deviation here divides by n, as NumPy and scikit-learn do; pandas .std() divides by n - 1 and would give 500 instead of 408.2 in the example.',
    prerequisites: ['feature-representation', 'gradient-descent-method', 'least-squares-normal-equation'],
    followOns: ['multicollinearity', 'ridge-regularization', 'lasso', 'train-validation-test'],
    sources: ['Production ML Slides Lesson 3 - Linear Regression.pdf', 'Huyen Ch. 5', 'James et al. §6.2.1'],
  },
  {
    id: 'bias-variance',
    title: 'The Bias–Variance Trade-off',
    group: 'Generalization',
    week: 'Production ML W2 · ISL §2.2.2 · Bishop §3.2',
    problem: 'It explains why test error is U-shaped in model complexity: error comes from a model that is too rigid (bias), one too sensitive to its particular training set (variance), and noise that no model can remove.',
    intuition: 'Train the same kind of model on many different training sets and look at its predictions at one input. Bias is how far their average is from the truth; variance is how much they scatter.',
    formulas: [
      { tex: tex`\mathbb{E}\bigl[(y-\hat h(x))^2\bigr]=\underbrace{\bigl(\mathbb{E}[\hat h(x)]-f(x)\bigr)^2}_{\text{bias}^2}+\underbrace{\mathbb{E}\bigl[(\hat h(x)-\mathbb{E}[\hat h(x)])^2\bigr]}_{\text{variance}}+\underbrace{\sigma^2}_{\text{noise}}`, definitions: [tex`f(x): the true regression function`, tex`\hat h(x): prediction of a model trained on a random training set`, tex`\mathbb{E}: average over training sets (and over the label noise)`, tex`\sigma^2: noise variance, the irreducible error`] },
      { tex: tex`y=f(x)+\varepsilon,\qquad \mathbb{E}[\varepsilon]=0,\quad \operatorname{Var}(\varepsilon)=\sigma^2`, definitions: [tex`\varepsilon: label noise, independent of the training set`] },
    ],
    example: ['The true value at some input is f(x) = 2. A simple model trained on three different training sets predicts 1.5, 1.6, and 1.4 there.', 'Its average prediction is 1.5, so bias^2 = (1.5 - 2)^2 = 0.25, and its variance is (0^2 + 0.1^2 + (-0.1)^2)/3 ≈ 0.0067.', 'A flexible model predicts 1.2, 2.9, and 2.0. Its average is about 2.033, so bias^2 ≈ 0.0011, but its variance is ((-0.833)^2 + 0.867^2 + (-0.033)^2)/3 ≈ 0.482.', 'Ignoring noise, the simple model\'s expected squared error is 0.25 + 0.0067 ≈ 0.257 and the flexible model\'s is 0.0011 + 0.482 ≈ 0.483. Here the low-bias model loses because of its variance.'],
    graph: { type: 'biasVariance', title: 'Bias squared, variance, and test error by degree', caption: 'Exact expected values (the average over all possible training sets) for polynomials of degree 0 to 9 fitted by least squares to 12 evenly spaced inputs of a sine curve with noise of standard deviation 0.4, measured at those inputs with fresh noise. Bias squared (blue) never increases with degree (even degrees add nothing here because this sine is odd about x = 0.5), variance (orange) rises, and their sum plus the noise (dark) is roughly U-shaped, lowest at degree 3.', sliders: [{ key: 'degree', label: 'polynomial degree', min: 0, max: 9, step: 1, value: 3 }] },
    figure: 'bias-variance-targets',
    misconception: 'Variance here is not the spread of the data. It is how much the fitted model would change if it were trained on a different sample, so a model can have low training error and still high variance.',
    prerequisites: ['model-complexity-generalization', 'polynomial-regression'],
    followOns: ['ridge-regularization', 'train-validation-test', 'cross-validation'],
    sources: ['Week2_notes01-LinearRegression.pdf', 'Production ML Slides Lesson 3 - Linear Regression.pdf', 'James et al. §2.2.2', 'Bishop §3.2'],
  },
  {
    id: 'roc-auc',
    title: 'ROC Curves and AUC',
    group: 'Generalization',
    week: 'Production ML W2 · ISL §4.4 · Huyen Ch. 4',
    problem: 'It shows how a classifier trades catching positives against raising false alarms as the threshold moves, and summarizes how well its scores rank positives above negatives in one number.',
    intuition: 'Slide the decision threshold from strict to lenient and watch two numbers: how many positives you catch, and how many false alarms you raise. The ROC curve plots that trade-off for every threshold at once.',
    formulas: [
      { tex: tex`\operatorname{TPR}=\frac{TP}{TP+FN},\qquad \operatorname{FPR}=\frac{FP}{FP+TN}`, definitions: [tex`\operatorname{TPR}: true positive rate (recall, sensitivity)`, tex`\operatorname{FPR}: false positive rate (1 - specificity)`] },
      { tex: tex`\operatorname{AUC}=P\bigl(s(x^+)>s(x^-)\bigr)\quad(\text{ties count }\tfrac12)`, definitions: [tex`s(x^+): score of a randomly chosen positive example`, tex`s(x^-): score of a randomly chosen negative example`] },
    ],
    example: ['Scores for three positives: 0.9, 0.8, 0.4. Scores for three negatives: 0.7, 0.3, 0.2.', 'Threshold 0.5: two positives score above it (0.9 and 0.8), so TPR = 2/3; one negative does (0.7), so FPR = 1/3.', 'Threshold 0.35 gives TPR = 3/3 = 1 and FPR = 1/3. Threshold 0.75 gives TPR = 2/3 and FPR = 0.', 'AUC: of the 3 x 3 = 9 positive-negative pairs, the positive scores higher in 8 (only 0.4 < 0.7 fails), so AUC = 8/9 ≈ 0.889.'],
    graph: { type: 'roc', title: 'Threshold, score distributions, and the ROC curve', caption: 'Right: scores of negatives (orange) and positives (blue), each normally distributed, and the threshold (dashed). Left: the ROC curve and the point for the current threshold. More separation between the classes pushes the curve toward the top-left and raises AUC.', sliders: [{ key: 'separation', label: 'class separation', min: 0, max: 3, step: 0.1, value: 1.5 }, { key: 'threshold', label: 'threshold', min: -2, max: 4, step: 0.1, value: 0.8 }] },
    misconception: 'A high AUC does not mean the classifier works well at your chosen threshold, and it can hide poor precision when positives are rare. It measures how well scores rank positives above negatives.',
    prerequisites: ['classification-metrics', 'logistic-regression'],
    followOns: ['ml-in-production'],
    sources: ['Production ML Slides Lesson 4 - Logistic Regression.pdf', 'James et al. §4.4', 'Huyen Ch. 4'],
  },
  {
    id: 'ml-in-production',
    title: 'ML in Production: Lifecycle, Monitoring, and Technical Debt',
    group: 'Problem',
    week: 'Production ML W1 · Huyen Ch. 1, 2, 8 · Sculley et al.',
    problem: 'It explains what it takes for a model to keep working after training: clear objectives, reliable data pipelines, deployment, monitoring for changes in the data, and retraining, without hidden maintenance costs piling up.',
    intuition: 'In a course, a model is done when its test error is low. In production, that is where the work starts: the model is a small box inside a large system, and the world it predicts keeps changing.',
    formulas: [
      { tex: tex`P_{\text{train}}(x,y)\ne P_{\text{prod}}(x,y)`, definitions: [tex`P_{\text{train}}: distribution of the training data`, tex`P_{\text{prod}}: distribution of the data seen in production; a difference is a distribution shift`] },
      { tex: tex`\text{covariate shift: } P(x)\text{ changes, } P(y\mid x)\text{ stays};\qquad \text{concept drift: } P(y\mid x)\text{ changes}`, definitions: [tex`P(y\mid x): how labels depend on inputs`, 'covariate shift: often called data drift or feature drift in industry, as in the Lesson 1 slides'] },
      { tex: tex`\text{label shift: } P(y)\text{ changes, } P(x\mid y)\text{ stays}`, definitions: ['label shift: the class mix changes, for example a disease becoming more common, the third type of shift in Huyen Ch. 8'] },
      { tex: tex`\bar A=A_0-\frac{d\,T}{2}`, definitions: [tex`\bar A: average accuracy when accuracy falls d points per month and the model is retrained every T months`, tex`A_0: accuracy right after training`, 'toy model: accuracy falls linearly and each retrain fully restores A_0; real decay is rarely this regular'] },
    ],
    example: ['A fraud model reaches 92% accuracy offline. After deployment, fraud patterns change and accuracy drops by about 1.5 points per month.', 'Never retrained, it is down to 92 - 1.5*12 = 74% after a year.', 'Retrained every 3 months, it moves between 92% and 87.5%, averaging 92 - 1.5*3/2 = 89.75%. Retrained monthly, it averages 91.25%, but needs about three times as many retrains.', 'Monitoring the input distribution and, once labels arrive, the live accuracy shows when to retrain instead of guessing.'],
    graph: { type: 'driftRetrain', title: 'Accuracy decay and retraining', caption: 'Accuracy of a deployed model over two years when the data drifts and the model is retrained every T months (T = 24 means never). Frequent retraining keeps accuracy high but costs more compute and engineering time.', sliders: [{ key: 'decay', label: 'accuracy lost per month (points)', min: 0, max: 3.5, step: 0.1, value: 1.5 }, { key: 'interval', label: 'retrain every T months', min: 1, max: 24, step: 1, value: 3 }] },
    figure: 'ml-lifecycle',
    misconception: 'A model with excellent test accuracy is not finished. Test accuracy measures one snapshot of the data; without monitoring, a shift in the data can make the deployed model quietly worse.',
    prerequisites: ['ml-landscape', 'ml-workflow', 'train-validation-test', 'classification-metrics'],
    followOns: [],
    sources: ['Production ML slides - Introduction Lesson1.pdf', 'Huyen Ch. 1, 2, 8', 'Sculley et al. (2014)'],
  },
  {
    id: 'multicollinearity',
    title: 'Multicollinearity and Conditioning',
    group: 'Complexity',
    week: 'Production ML W2 (supplementary)',
    problem: 'It explains why least-squares weights can swing wildly, or even flip sign, when features carry almost the same information, and how to detect and fix it.',
    intuition: 'If two features always move together, the data show what they do jointly but not how to split the credit between them. Many pairs of weights fit almost equally well, so a small change in the data can move the weights a long way.',
    formulas: [
      { tex: tex`C=\tfrac1nZ^TZ=\sum_{i=1}^{d}\lambda_i\,v_iv_i^T,\qquad \hat\theta=C^{-1}b=\sum_{i=1}^{d}\frac{v_i^Tb}{\lambda_i}\,v_i`, definitions: [tex`Z: standardized design matrix (n by d, y centred), so C is the correlation matrix of the features`, tex`\lambda_i, v_i: eigenvalues and orthonormal eigenvectors of C`, tex`b: (1/n) Z^T y, how strongly y lines up with each feature`] },
      { tex: tex`\operatorname{Cov}(\hat\theta)=\frac{\sigma^2}{n}\,C^{-1}=\frac{\sigma^2}{n}\sum_{i=1}^{d}\frac{v_iv_i^T}{\lambda_i},\qquad \kappa=\frac{\lambda_{\max}}{\lambda_{\min}}`, definitions: [tex`\sigma^2: noise variance of y around the true linear model`, tex`\kappa: condition number; a small eigenvalue lambda_i means the features barely vary along v_i, so the weights along v_i have variance sigma^2/(n lambda_i)`] },
      { tex: tex`\operatorname{VIF}_j=\bigl(C^{-1}\bigr)_{jj}=\frac{1}{1-R_j^2}`, definitions: [tex`R_j^2: R^2 of predicting feature j from all the other features`, tex`\operatorname{VIF}_j: variance inflation factor, how many times collinearity multiplies the variance of weight j; above about 10 is a common warning sign`] },
      { tex: tex`\hat\theta_{\text{ridge}}=\sum_{i=1}^{d}\frac{\lambda_i}{\lambda_i+\lambda}\,\frac{v_i^Tb}{\lambda_i}\,v_i,\qquad \kappa_{\text{ridge}}=\frac{\lambda_{\max}+\lambda}{\lambda_{\min}+\lambda}`, definitions: [tex`\lambda: the ridge penalty, added to every eigenvalue (lambda_i with a subscript is an eigenvalue)`, tex`\frac{\lambda_i}{\lambda_i+\lambda}: shrink factor, close to 1 for well-measured directions and close to 0 for poorly measured ones`] },
    ],
    example: [
      'Two standardized features with correlation 0.95, such as total cholesterol and LDL cholesterol: C = [[1, 0.95], [0.95, 1]].',
      'Its eigenvalues are 1.95 along v_1 = [1, 1]/sqrt 2 (the features moving together) and 0.05 along v_2 = [1, -1]/sqrt 2 (their difference), so kappa = 1.95/0.05 = 39.',
      'The weights are pinned down well along v_1, with variance sigma^2/(1.95 n) ≈ 0.51 sigma^2/n, but poorly along v_2, with variance sigma^2/(0.05 n) = 20 sigma^2/n: the data fix the sum of the two weights, not how it is split.',
      'Each weight on its own: VIF = 1/(1 - 0.95^2) ≈ 10.3, so its variance is about 10 times what it would be with uncorrelated features.',
      'Ridge with lambda = 0.1 shrinks the weak direction by 0.05/0.15 ≈ 0.33 and the strong one only by 1.95/2.05 ≈ 0.95. The condition number falls from 39 to 2.05/0.15 ≈ 13.7.',
      'On the real diabetes data of the project below: smallest eigenvalue 0.0088, kappa 464, VIFs up to 58 for total cholesterol, and ridge with lambda = 0.42 brings kappa down to 10.',
    ],
    graph: { type: 'collinearity', title: 'Correlated features make the weights unstable', caption: 'Each grey dot is the least-squares estimate of two weights from a different simulated dataset; the true weights are (2, 1). As the correlation rho grows, the dots spread out along the dashed line, the direction [1, -1] in which the two features barely differ. Raise lambda to turn on ridge (blue dots): the spread collapses, but the centre is pulled away from the truth, the bias-variance trade.', sliders: [{ key: 'rho', label: 'feature correlation rho', min: 0, max: 0.99, step: 0.01, value: 0.95 }, { key: 'penalty', label: 'ridge penalty lambda', min: 0, max: 1, step: 0.01, value: 0 }] },
    misconception: 'Multicollinearity does not make predictions bad. For new data that look like the training data the predictions stay accurate, because the uncertain direction is one the features barely vary along; what becomes unreliable is each individual weight, and any story told from it. Dropping a feature is one fix but throws information away; ridge keeps every feature and stabilizes how the credit is split.',
    prerequisites: ['least-squares-normal-equation', 'feature-scaling', 'eigenvalues-eigenvectors'],
    followOns: ['ridge-regularization', 'bias-variance', 'bootstrap'],
    sources: ['James et al. §3.3.3 (collinearity and the VIF; supplementary)', 'Mathematics for Machine Learning §4.4 and §9.2'],
  },
  {
    id: 'bootstrap',
    title: 'The Bootstrap: How Much Would the Answer Change?',
    group: 'Generalization',
    week: 'Production ML W2 (supplementary)',
    problem: 'It measures how much a number computed from data (a mean, a model weight, a test score, or the gap between two models) would change with a different sample, using only the one dataset you have.',
    intuition: 'We cannot collect a fresh dataset a thousand times, so we let the data stand in for the world. Draw n examples from it with replacement, recompute the number, and repeat. The spread of the recomputed numbers shows how much the real one could wobble.',
    formulas: [
      { tex: tex`x^{*(b)}=\bigl(x^{(i_1)},\ldots,x^{(i_n)}\bigr),\qquad i_1,\ldots,i_n\ \text{drawn uniformly from }\{1,\ldots,n\}\text{ with replacement}`, definitions: [tex`x^{*(b)}: the b-th bootstrap resample, the same size n as the data; some examples repeat and some are missing`, tex`b: resample number, b = 1, ..., B, with B typically 1000 or more`] },
      { tex: tex`\widehat{\operatorname{SE}}_{\text{boot}}=\sqrt{\frac{1}{B-1}\sum_{b=1}^{B}\bigl(\hat\theta^{*(b)}-\bar\theta^{*}\bigr)^2}`, definitions: [tex`\hat\theta^{*(b)}: the statistic (a mean, a weight, a test RMSE, an AUC) recomputed on resample b`, tex`\bar\theta^{*}: the average of the B recomputed values`] },
      { tex: tex`\Bigl[\hat\theta^{*}_{(0.025)},\ \hat\theta^{*}_{(0.975)}\Bigr]`, definitions: ['percentile interval: the 2.5th and 97.5th percentiles of the B recomputed values, an approximate 95% confidence interval'] },
      { tex: tex`P\bigl(\text{example }t\text{ is not in a resample}\bigr)=\Bigl(1-\frac1n\Bigr)^{n}\ \to\ e^{-1}\approx0.368`, definitions: ['out-of-bag examples: the ones a resample leaves out, about 36.8% for large n; each resample contains about 63.2% of the distinct examples'] },
    ],
    example: [
      'A tiny sample: 2, 4, 9, with mean 5. How much would the mean change with three new values?',
      'Draw 3 values from {2, 4, 9} with replacement: for example (2, 9, 9), with mean 6.67, or (4, 4, 2), with mean 3.33. There are 3^3 = 27 equally likely ordered resamples.',
      'Over all 27 the means have variance (26/3)/3 ≈ 2.89, so the bootstrap standard error is about 1.70. The formula s/sqrt(n) = sqrt(13/3) ≈ 2.08 is larger because it divides by n - 1; for larger n the two agree.',
      'Each value is missing from a resample with probability (2/3)^3 ≈ 0.30. For large n this tends to e^-1 ≈ 0.368.',
      'Comparing two models on one test set: resample the test patients and score both models on the same resample. In the diabetes project, RMSE(OLS) - RMSE(ridge) = -0.12 with 95% interval [-2.54, 2.00]. The interval contains 0, so the models are tied.',
    ],
    graph: { type: 'bootstrap', title: 'Resampling one sample shows how much its mean could vary', caption: 'One fixed, skewed sample of size n. Each bar counts resamples whose mean fell in that range; the blue line is the sample mean and the orange lines bound the middle 95%. Larger n narrows the spread like 1/sqrt(n); more resamples B only make the picture smoother, not narrower.', sliders: [{ key: 'n', label: 'sample size n', min: 5, max: 80, step: 1, value: 20 }, { key: 'B', label: 'number of resamples B', min: 100, max: 2000, step: 100, value: 1000 }] },
    misconception: 'The bootstrap does not create new information. It shows the variation you would see if the world looked like your sample, so it cannot fix a biased sample, and with very small n its intervals are too narrow. Resample the units that are independent: whole patients rather than single measurements, blocks rather than single days of a time series.',
    prerequisites: ['expectation-variance', 'cross-validation'],
    followOns: ['classification-metrics', 'roc-auc'],
    sources: ['James et al. §5.2 (supplementary)', 'Bishop §1.2.3', 'Duda, Hart & Stork §9.4.2'],
  },
];
