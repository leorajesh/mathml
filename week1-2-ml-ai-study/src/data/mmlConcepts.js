// Concept pages added to follow the course reference book, "Mathematics for Machine Learning"
// (Deisenroth, Faisal, Ong): norms and inner products, orthogonal complements, projections and
// Gram-Schmidt (Ch. 3), trace (Ch. 4), momentum and Lagrange multipliers (Ch. 7), and PCA (Ch. 10).
// The explanations, numbers, and pictures are our own; the book is cited on each page.

const tex = String.raw;

export const mmlConcepts = [
  {
    id: 'norms',
    title: 'Norms: Measuring the Length of a Vector',
    group: 'Representation',
    week: 'MML book §3.1',
    problem: 'It gives a precise meaning to "how big is this vector?", which is what error sizes, distances, and regularization penalties all measure.',
    intuition: 'A norm answers "how big is this vector?". Error sizes, distances, and regularization penalties are all norms, and different norms measure size in different ways.',
    formulas: [
      { tex: tex`\lVert x\rVert_1=\sum_{i=1}^{n}|x_i|,\qquad \lVert x\rVert_2=\sqrt{\sum_{i=1}^{n}x_i^2}=\sqrt{x^Tx},\qquad \lVert x\rVert_\infty=\max_i|x_i|`, definitions: [tex`x_i: the i-th coordinate of x`, tex`\lVert x\rVert_1: Manhattan (L1) norm`, tex`\lVert x\rVert_2: Euclidean (L2) norm, the usual length`, tex`\lVert x\rVert_\infty: max norm`] },
      { tex: tex`\lVert x\rVert=0\iff x=0,\qquad \lVert c\,x\rVert=|c|\,\lVert x\rVert,\qquad \lVert x+y\rVert\le\lVert x\rVert+\lVert y\rVert`, definitions: [tex`c: any real number`, 'triangle inequality: the last rule, a detour is never shorter than the direct route'] },
      { tex: tex`d(x,y)=\lVert x-y\rVert,\qquad \lVert x\rVert_\infty\le\lVert x\rVert_2\le\lVert x\rVert_1`, definitions: [tex`d(x,y): the distance between x and y that a norm defines`] },
    ],
    example: ['Take x = [3, -4].', 'L1 norm: |3| + |-4| = 7.', 'L2 norm: sqrt(3^2 + (-4)^2) = sqrt(25) = 5.', 'Max norm: the largest absolute coordinate, 4. As always, max <= L2 <= L1: here 4 <= 5 <= 7.', 'Triangle inequality with y = [1, 1]: ||x + y||_2 = ||[4, -3]||_2 = 5, which is at most ||x||_2 + ||y||_2 = 5 + 1.414.'],
    graph: { type: 'normBall', title: 'Unit balls and lengths in three norms', caption: 'The faint outlines are the unit "circles" (length 1) of the three norms. The bold outline is every vector with the same length as the point in the chosen norm, so it always passes through the point.', sliders: [{ key: 'norm', label: 'norm (1 = L1, 2 = L2, 3 = max)', min: 1, max: 3, step: 1, value: 1 }, { key: 'px', label: 'point x', min: -4, max: 4, step: 0.25, value: 3 }, { key: 'py', label: 'point y', min: -4, max: 4, step: 0.25, value: -4 }] },
    figure: 'l1-l2-balls',
    misconception: 'A vector does not have just one length: it depends on the norm. [3, -4] has L2 length 5 but L1 length 7, and the L1 "unit circle" is a diamond, not a circle.',
    prerequisites: ['vectors-dot-product'],
    followOns: ['inner-products', 'lasso', 'ridge-regularization'],
    sources: ['Mathematics for Machine Learning, §3.1'],
  },
  {
    id: 'inner-products',
    title: 'Inner Products, Lengths, and Angles',
    group: 'Representation',
    week: 'MML book §3.2-3.4',
    problem: 'It generalizes the dot product so that lengths, distances, and angles can be measured in the way that suits a problem, for example weighting some directions more than others.',
    intuition: 'The dot product is one way to turn two vectors into a number, but not the only one. An inner product is any such rule that behaves well, and each one comes with its own ruler for lengths and angles.',
    formulas: [
      { tex: tex`\langle x,y\rangle=x^TAy,\qquad A=A^T,\quad x^TAx>0\ \text{for all}\ x\ne0`, definitions: [tex`\langle x,y\rangle: inner product of x and y`, tex`A: symmetric positive definite (SPD) matrix; A = I gives the dot product`] },
      { tex: tex`\lVert x\rVert=\sqrt{\langle x,x\rangle},\qquad d(x,y)=\lVert x-y\rVert,\qquad \cos\omega=\frac{\langle x,y\rangle}{\lVert x\rVert\,\lVert y\rVert}`, definitions: [tex`\lVert x\rVert: length that the inner product induces`, tex`\omega: angle between x and y`] },
      { tex: tex`|\langle x,y\rangle|\le\lVert x\rVert\,\lVert y\rVert`, definitions: ['Cauchy-Schwarz inequality: keeps cos(omega) between -1 and 1, so angles always make sense'] },
    ],
    example: ['Use A = [[2, 1], [1, 2]]. It is symmetric, and x^T A x = 2x1^2 + 2x1x2 + 2x2^2 = x1^2 + x2^2 + (x1 + x2)^2, which is positive unless x = 0, so <x, y> = x^T A y is an inner product.', 'For x = [1, 0] and y = [0, 1], <x, y> = A_12 = 1. Under the dot product they are perpendicular (x . y = 0), but under this inner product they are not.', 'Lengths: ||x|| = sqrt(x^T A x) = sqrt(2), and likewise ||y|| = sqrt(2).', 'Angle: cos(omega) = 1 / (sqrt(2) sqrt(2)) = 0.5, so omega = 60 degrees.', 'A vector orthogonal to x = [1, 0] under A needs <x, z> = 2z1 + z2 = 0, for example z = [1, -2], which does not look perpendicular in the usual picture.'],
    graph: { type: 'innerProductBall', title: 'The unit circle of x^T A y', caption: 'The dashed circle is the unit circle of the dot product. The solid ellipse is every vector of length 1 under the inner product x^T A y. Both are drawn 3 times larger than life. When A stops being positive definite, there is no inner product.', sliders: [{ key: 'a', label: 'A_11', min: -1, max: 4, step: 0.25, value: 2 }, { key: 'b', label: 'A_12 = A_21', min: -2, max: 2, step: 0.25, value: 1 }, { key: 'd', label: 'A_22', min: -1, max: 4, step: 0.25, value: 2 }] },
    misconception: 'Orthogonality is not a fixed property of two vectors; it depends on the inner product. [1, 0] and [0, 1] are orthogonal under the dot product but meet at 60 degrees under x^T A y with A = [[2, 1], [1, 2]].',
    prerequisites: ['norms', 'matrix-multiplication-outer-product'],
    followOns: ['orthogonality', 'orthogonal-projections', 'cholesky-decomposition'],
    sources: ['Mathematics for Machine Learning, §3.2-3.4'],
  },
  {
    id: 'orthogonal-complement',
    title: 'Orthogonal Complements and the Four Fundamental Subspaces',
    group: 'Representation',
    week: 'MML book §3.6, §2.7.3',
    problem: 'It splits space into a subspace and everything perpendicular to it, which organizes the null space and column space of every matrix and explains least-squares errors.',
    intuition: 'Take a subspace U. Its orthogonal complement is everything perpendicular to all of U, like the normal line to a plane through the origin in 3D.',
    formulas: [
      { tex: tex`U^\perp=\{v:\ v\cdot u=0\ \text{for all}\ u\in U\},\qquad \dim U+\dim U^\perp=n`, definitions: [tex`U^\perp: orthogonal complement of the subspace U`, tex`n: dimension of the whole space`] },
      { tex: tex`\operatorname{Null}(A)=\operatorname{Row}(A)^\perp\subseteq\mathbb{R}^n,\qquad \operatorname{Null}(A^T)=\operatorname{Col}(A)^\perp\subseteq\mathbb{R}^m`, definitions: [tex`\operatorname{Row}(A): row space, spanned by the rows of A`, tex`\operatorname{Null}(A^T): left null space, the vectors y with A^T y = 0`, tex`\operatorname{Col}(A): column space`] },
      { tex: tex`\underbrace{r}_{\text{row}}+\underbrace{n-r}_{\text{null}}=n,\qquad \underbrace{r}_{\text{column}}+\underbrace{m-r}_{\text{left null}}=m`, definitions: [tex`r: rank of A, the dimension of both the row space and the column space`] },
    ],
    example: ['Take A = [[1, 2, 3], [2, 4, 6]], the 2 by 3 matrix of rank r = 1 from the rank-nullity page.', 'Row space: multiples of [1, 2, 3], a line in R^3. Null space: spanned by [-2, 1, 0] and [-3, 0, 1], a plane in R^3.', 'They are perpendicular: [1, 2, 3] . [-2, 1, 0] = 0 and [1, 2, 3] . [-3, 0, 1] = 0. Dimensions: 1 + 2 = 3 = n.', 'Column space: multiples of [1, 2] in R^2. Left null space: A^T y = 0 means y1 + 2y2 = 0, spanned by [-2, 1], which is perpendicular to [1, 2]. Dimensions: 1 + 1 = 2 = m.', 'Split x = [1, 0, 0]: its row-space part is (x . a / a . a) a = [1, 2, 3]/14 for the row a = [1, 2, 3], and the rest, [13, -2, -3]/14, is in the null space: 13 - 4 - 9 = 0.'],
    graph: { type: 'complement', title: 'A line and its orthogonal complement', caption: 'In the plane, the complement of a line through the origin is the perpendicular line. Every point splits into one part on each, and the squared lengths add up (Pythagoras).', sliders: [{ key: 'theta', label: 'angle of U', min: 0, max: 180, step: 5, value: 30 }, { key: 'px', label: 'point x', min: -4, max: 4, step: 0.25, value: 1 }, { key: 'py', label: 'point y', min: -4, max: 4, step: 0.25, value: 3 }] },
    figure: 'four-subspaces',
    misconception: 'The orthogonal complement is not the set of vectors outside U. It is itself a subspace (it contains 0), and most vectors lie in neither U nor U-perp; they are a sum of one part from each.',
    prerequisites: ['orthogonality', 'rank-nullity'],
    followOns: ['orthogonal-projections', 'least-squares-normal-equation'],
    sources: ['Mathematics for Machine Learning, §3.6 and §2.7.3'],
  },
  {
    id: 'orthogonal-projections',
    title: 'Orthogonal Projections onto Lines and Subspaces',
    group: 'Representation',
    week: 'MML book §3.8',
    problem: 'It finds the closest point in a subspace to a given vector, the key step in least squares, PCA, and compression.',
    intuition: 'Drop a perpendicular from a point to a line or plane: where it lands is the orthogonal projection, the closest point of that subspace.',
    formulas: [
      { tex: tex`\pi_b(x)=\frac{b^Tx}{b^Tb}\,b=\frac{bb^T}{b^Tb}\,x`, definitions: [tex`b: nonzero vector spanning the line`, tex`\pi_b(x): projection of x onto that line`] },
      { tex: tex`B^T(x-B\lambda)=0\ \Rightarrow\ \lambda=(B^TB)^{-1}B^Tx,\qquad \pi_U(x)=B\lambda`, definitions: [tex`B: matrix whose linearly independent columns are a basis of the subspace U`, tex`\lambda: coordinates of the projection in that basis`] },
      { tex: tex`P=B(B^TB)^{-1}B^T,\qquad P^2=P,\qquad P^T=P`, definitions: [tex`P: projection matrix onto U; with orthonormal columns it simplifies to B B^T`] },
    ],
    example: ['Project x = [1, 2, 6] onto the plane spanned by b1 = [1, 0, 1] and b2 = [0, 1, 1].', 'Normal equation: B^T B = [[2, 1], [1, 2]] and B^T x = [1 + 6, 2 + 6] = [7, 8].', 'Solve: lambda = (B^T B)^(-1) B^T x = (1/3)[[2, -1], [-1, 2]][7, 8] = [2, 3].', 'The projection is 2 b1 + 3 b2 = [2, 3, 5], and the error is e = x - [2, 3, 5] = [-1, -1, 1].', 'Check: e . b1 = -1 + 1 = 0 and e . b2 = -1 + 1 = 0, so the error is perpendicular to the plane, and its length sqrt(3) is the distance from x to the plane.'],
    graph: { type: 'lineProjection', title: 'Projection onto a line', caption: 'The red arrow is the projection of x onto the line (the grey line through the origin); the dashed error is perpendicular to the line. No other point on the line is closer to x.', sliders: [{ key: 'theta', label: 'line angle', min: 0, max: 180, step: 5, value: 20 }, { key: 'px', label: 'point x', min: -5, max: 5, step: 0.25, value: 2 }, { key: 'py', label: 'point y', min: -5, max: 5, step: 0.25, value: 3 }] },
    figure: 'projection-plane',
    misconception: 'Projecting is not the same as deleting coordinates. Setting the third coordinate of [1, 2, 6] to zero gives [1, 2, 0], the projection onto the xy-plane only. For any other subspace you need the normal equation or an orthonormal basis.',
    prerequisites: ['orthogonal-complement', 'invertible-transformations'],
    followOns: ['gram-schmidt', 'least-squares-normal-equation', 'dimensionality-reduction', 'pca'],
    sources: ['Mathematics for Machine Learning, §3.8'],
  },
  {
    id: 'gram-schmidt',
    title: 'Gram-Schmidt Orthogonalization',
    group: 'Representation',
    week: 'MML book §3.8.3',
    problem: 'It turns any basis into an orthonormal basis of the same space, so that coordinates and projections become simple dot products.',
    intuition: 'Gram-Schmidt straightens out a basis: it turns any basis into perpendicular unit vectors that span the same space.',
    formulas: [
      { tex: tex`u_1=b_1,\qquad u_k=b_k-\sum_{j<k}\frac{u_j^Tb_k}{u_j^Tu_j}\,u_j`, definitions: [tex`b_k: the original basis vectors, in order`, tex`u_k: the orthogonal vectors built so far`] },
      { tex: tex`q_k=\frac{u_k}{\lVert u_k\rVert}`, definitions: [tex`q_k: the orthonormal basis vectors`] },
      { tex: tex`A=QR,\qquad R=Q^TA`, definitions: [tex`A: matrix whose columns a_k are the vectors to orthogonalize`, tex`Q: matrix with the orthonormal columns q_k`, tex`R: upper triangular, with entries q_j dot a_k`] },
    ],
    example: ['Start from b1 = [3, 1] and b2 = [2, 2], a basis of R^2 that is not orthogonal: b1 . b2 = 8.', 'Keep u1 = b1 = [3, 1].', 'Remove the part of b2 along u1: (u1 . b2)/(u1 . u1) = 8/10 = 0.8, so u2 = [2, 2] - 0.8 [3, 1] = [-0.4, 1.2].', 'Check: u1 . u2 = -1.2 + 1.2 = 0. Normalize: q1 = [3, 1]/sqrt(10) and q2 = [-0.4, 1.2]/sqrt(1.6) = [-1, 3]/sqrt(10).'],
    graph: { type: 'gramSchmidt', title: 'One Gram-Schmidt step', caption: 'b1 is the red arrow and b2 the blue one. The dashed path splits b2 into its projection onto b1 (along the grey line) and the leftover u2, drawn in orange from the origin, which is perpendicular to b1.', sliders: [{ key: 'theta', label: 'direction of b1', min: 0, max: 180, step: 5, value: 20 }, { key: 'bx', label: 'b2 x', min: -4, max: 4, step: 0.25, value: 2 }, { key: 'by', label: 'b2 y', min: -4, max: 4, step: 0.25, value: 2 }] },
    misconception: 'Gram-Schmidt does not give the one "right" orthonormal basis: the result depends on the order of the input vectors. In floating point the classical version slowly loses orthogonality, which is why libraries use modified Gram-Schmidt or Householder QR.',
    prerequisites: ['orthogonal-projections'],
    followOns: ['least-squares-normal-equation', 'eigenvalues-eigenvectors'],
    sources: ['Mathematics for Machine Learning, §3.8.3'],
  },
  {
    id: 'trace',
    title: 'Trace and the Sum of Eigenvalues',
    group: 'Representation',
    week: 'MML book §4.1-4.2',
    problem: 'It condenses a square matrix into one number, the sum of its diagonal, which equals the sum of its eigenvalues and does not change under a change of basis.',
    intuition: 'The trace is just the sum of the diagonal entries, yet it knows more than it seems: it equals the sum of the eigenvalues.',
    formulas: [
      { tex: tex`\operatorname{tr}(A)=\sum_{i=1}^{n}a_{ii}=\sum_{i=1}^{n}\lambda_i,\qquad \det(A)=\prod_{i=1}^{n}\lambda_i`, definitions: [tex`a_{ii}: diagonal entries of the n by n matrix A`, tex`\lambda_i: eigenvalues of A, counted with repeats (possibly complex)`] },
      { tex: tex`\operatorname{tr}(A+B)=\operatorname{tr}(A)+\operatorname{tr}(B),\qquad \operatorname{tr}(AB)=\operatorname{tr}(BA),\qquad \operatorname{tr}(P^{-1}AP)=\operatorname{tr}(A)`, definitions: [tex`B: matrix such that the products AB and BA are both defined`, tex`P: any invertible matrix (a change of basis)`] },
      { tex: tex`\lambda^2-\operatorname{tr}(A)\,\lambda+\det(A)=0\quad(2\times2)`, definitions: [tex`\lambda: an eigenvalue of a 2 by 2 matrix A`] },
      { tex: tex`A\in\mathbb{R}^{m\times n},\ B\in\mathbb{R}^{n\times m}:\ \ \operatorname{tr}(AB)=\sum_{i=1}^{m}\sum_{\ell=1}^{n}a_{i\ell}b_{\ell i}=\sum_{\ell=1}^{n}\sum_{i=1}^{m}b_{\ell i}a_{i\ell}=\operatorname{tr}(BA)`, definitions: ['general case: AB is m by m and BA is n by n, different sizes, yet their traces agree; the proof swaps the two finite sums and uses that numbers commute (Tr and tr are the same thing)'] },
    ],
    example: ['Let A = [[4, 1], [2, 3]]. Then tr(A) = 4 + 3 = 7 and det(A) = 4*3 - 1*2 = 10.', 'The characteristic equation is lambda^2 - 7 lambda + 10 = 0, which factors as (lambda - 5)(lambda - 2) = 0.', 'So the eigenvalues are 5 and 2: their sum 7 is the trace and their product 10 is the determinant.', 'Cyclic rule with B = [[1, 0], [1, 1]]: AB = [[5, 1], [5, 3]] and BA = [[4, 1], [6, 4]] are different matrices, but both have trace 8.', 'Rectangular case: A = [2 1] (1 by 2) and B = [1; 3] (2 by 1). AB = [5], a 1 by 1 matrix with trace 5; BA = [[2,1],[6,3]], a 2 by 2 matrix with trace 2 + 3 = 5.'],
    graph: { type: 'traceEigen', title: 'Trace = sum of eigenvalues', caption: 'Change the entries of a 2 by 2 matrix. The eigenvalues (the dots on the number line) always add up to the trace and multiply to the determinant; when the discriminant is negative they are a complex pair.', sliders: [{ key: 'a', label: 'a', min: -3, max: 5, step: 0.5, value: 4 }, { key: 'b', label: 'b', min: -3, max: 3, step: 0.5, value: 1 }, { key: 'c', label: 'c', min: -3, max: 3, step: 0.5, value: 2 }, { key: 'd', label: 'd', min: -3, max: 5, step: 0.5, value: 3 }] },
    misconception: 'The trace does not multiply: tr(AB) is usually not tr(A) tr(B). And it is only cyclic, so tr(ABC) = tr(CAB), but tr(ACB) can be different.',
    prerequisites: ['eigenvalues-eigenvectors'],
    followOns: ['diagonalization', 'pca'],
    sources: ['Mathematics for Machine Learning, §4.1-4.2'],
  },
  {
    id: 'pca',
    title: 'Principal Component Analysis (PCA)',
    group: 'Generalization',
    week: 'MML book Ch. 10',
    problem: 'It finds the few directions in which data varies most, so high-dimensional data can be compressed, visualized, or denoised while losing as little as possible.',
    intuition: 'PCA finds the few directions in which data varies most, so it can be compressed, visualized, or denoised while losing as little as possible.',
    formulas: [
      { tex: tex`S=\frac{1}{N}\sum_{n=1}^{N}x_nx_n^T`, definitions: [tex`S: data covariance matrix`, tex`x_n: a data point with the mean already subtracted`, tex`N: number of data points`] },
      { tex: tex`\max_{\lVert b\rVert=1}\ b^TSb=\lambda_1,\ \text{reached at}\ b=b_1`, definitions: [tex`b: a unit direction to project onto`, tex`\lambda_1: largest eigenvalue of S, the variance kept`, tex`b_1: its unit eigenvector, the first principal component`] },
      { tex: tex`z_n=B^Tx_n,\quad \tilde{x}_n=Bz_n,\qquad \frac{1}{N}\sum_{n=1}^{N}\lVert x_n-\tilde{x}_n\rVert^2=\sum_{j=M+1}^{D}\lambda_j`, definitions: [tex`B: D by M matrix whose columns are the top M eigenvectors of S`, tex`z_n: the M-number code of x_n`, tex`\tilde{x}_n: the reconstruction; its average squared error is the sum of the eigenvalues left out`] },
      { tex: tex`\text{explained variance}=\frac{\lambda_1+\dots+\lambda_M}{\operatorname{tr}(S)}`, definitions: [tex`\operatorname{tr}(S): total variance, the sum of all eigenvalues`] },
    ],
    example: ['Take four points whose mean is already 0: (2, 2), (-2, -2), (1, -1), (-1, 1).', 'Covariance: S = (1/4) sum of x x^T = (1/4)[[10, 6], [6, 10]] = [[2.5, 1.5], [1.5, 2.5]].', 'tr(S) = 5 and det(S) = 6.25 - 2.25 = 4, so the eigenvalues are 4 and 1. The first principal component is b1 = [1, 1]/sqrt(2), with variance 4.', 'Codes z = b1 . x: 2.83, -2.83, 0, 0. Their mean square is (8 + 8 + 0 + 0)/4 = 4 = lambda_1.', 'Reconstructions z b1: (2, 2), (-2, -2), (0, 0), (0, 0). The average squared error is (0 + 0 + 2 + 2)/4 = 1 = lambda_2, and 4 + 1 = 5 = tr(S). One component explains 4/5 = 80% of the variance.'],
    graph: { type: 'pca', title: 'Maximum variance equals minimum error', caption: 'Turn the direction. The spread of the projected points (kept variance) and the red residuals (lost error) always add up to the same total, so the direction that keeps the most variance also loses the least.', sliders: [{ key: 'angle', label: 'projection direction', min: 0, max: 180, step: 1, value: 0 }, { key: 'tilt', label: 'data tilt', min: 0, max: 180, step: 5, value: 35 }] },
    misconception: 'PCA is not feature selection: each principal component mixes all the original features. It is also sensitive to units, so features measured on very different scales should be standardized first, or the largest-scale feature dominates.',
    prerequisites: ['dimensionality-reduction', 'spectral-theorem', 'orthogonal-projections', 'trace', 'covariance-gaussian'],
    followOns: ['svd', 'model-complexity-generalization'],
    sources: ['Mathematics for Machine Learning, Ch. 10'],
  },
  {
    id: 'momentum',
    title: 'Gradient Descent with Momentum',
    group: 'Optimization',
    week: 'MML book §7.1',
    problem: 'It speeds up gradient descent in long, narrow valleys, where plain steps zig-zag across the valley instead of moving along it.',
    intuition: 'In a long, narrow valley, plain gradient descent zig-zags across it and crawls along it. Momentum turns the optimizer into a heavy rolling ball that speeds up in the direction that keeps paying off.',
    formulas: [
      { tex: tex`x_{i+1}=x_i-\gamma\,\nabla f(x_i)+\alpha\,\Delta x_i,\qquad \Delta x_i=x_i-x_{i-1}`, definitions: [tex`\gamma: step size (learning rate)`, tex`\alpha: momentum coefficient, between 0 and 1; alpha = 0 is plain gradient descent`, 'notation: here gamma is the step size (alpha on the gradient descent page, eta in the course notes), and alpha is the momentum weight', 'stability: on a quadratic, heavy-ball momentum is stable when gamma lambda_max < 2(1 + alpha), a wider range than plain descent', tex`\Delta x_i: the previous step`] },
      { tex: tex`f(x)=\tfrac12\left(x_1^2+\kappa\,x_2^2\right):\quad \text{plain descent needs}\ \gamma<\tfrac{2}{\kappa}`, definitions: [tex`\kappa: how much steeper one direction is than the other (the condition number); a large kappa is a long, narrow valley`] },
    ],
    example: ['Minimize f(x) = x^2 (gradient 2x) from x_0 = 10 with step size gamma = 0.1. Plain gradient descent multiplies x by 0.8 each step: 10, 8, 6.4, 5.12.', 'With momentum alpha = 0.5, the first step has no history: x_1 = 10 - 0.1(20) = 8, so the step was Delta x_1 = -2.', 'x_2 = 8 - 0.1(16) + 0.5(-2) = 8 - 1.6 - 1 = 5.4, so Delta x_2 = -2.6.', 'x_3 = 5.4 - 0.1(10.8) + 0.5(-2.6) = 5.4 - 1.08 - 1.3 = 3.02, already much closer to 0 than plain descent (5.12).'],
    graph: { type: 'momentum', title: 'Zig-zag versus momentum in a narrow valley', caption: 'Contours of f = (x1^2 + 20 x2^2)/2. Grey: plain gradient descent. Orange: the same step size with momentum. Set alpha to 0 and the two paths coincide.', sliders: [{ key: 'gamma', label: 'step size gamma', min: 0.01, max: 0.12, step: 0.005, value: 0.09 }, { key: 'alpha', label: 'momentum alpha', min: 0, max: 0.95, step: 0.05, value: 0.5 }, { key: 'steps', label: 'steps', min: 1, max: 60, step: 1, value: 20 }] },
    figure: 'gd-zigzag',
    misconception: 'Momentum is not just a larger learning rate. It averages recent steps, which cancels the back-and-forth part of the gradient and builds speed in consistent directions. A larger step size alone would make the zig-zag worse, or diverge.',
    prerequisites: ['gradient-descent-method'],
    followOns: ['stochastic-subgradient-descent', 'lagrange-multipliers'],
    sources: ['Mathematics for Machine Learning, §7.1'],
  },
  {
    id: 'lagrange-multipliers',
    title: 'Constrained Optimization and Lagrange Multipliers',
    group: 'Optimization',
    week: 'MML book §7.2',
    problem: 'It finds the best point when the parameters must satisfy constraints, such as staying on a line or inside a ball of limited size.',
    intuition: 'With a constraint, you must stay on a path. Walk along it while the function keeps dropping, until the path just touches a level curve without crossing it: that touching point is the constrained minimum.',
    formulas: [
      { tex: tex`\min_x f(x)\ \text{s.t.}\ h(x)=0:\qquad \nabla f(x^*)+\lambda\,\nabla h(x^*)=0,\quad h(x^*)=0`, definitions: [tex`f: objective to minimize`, tex`h: equality constraint function`, tex`\lambda: Lagrange multiplier`, tex`x^*: the constrained minimizer`] },
      { tex: tex`L(x,\lambda)=f(x)+\lambda\,h(x),\qquad \nabla_xL=0,\quad \frac{\partial L}{\partial\lambda}=h(x)=0`, definitions: [tex`L: the Lagrangian`] },
      { tex: tex`\min f\ \text{s.t.}\ g(x)\le0:\quad \nabla f(x^*)+\lambda\nabla g(x^*)=0,\ \ g(x^*)\le0,\ \ \lambda\ge0,\ \ \lambda\,g(x^*)=0`, definitions: [tex`g: inequality constraint function`, 'complementary slackness: the last condition; either the constraint is active or its multiplier is zero', 'KKT conditions: necessary at a minimum (for a regular constraint); for convex f and g they are also sufficient'] },
    ],
    example: ['Minimize f(x, y) = x^2 + y^2 subject to x + y = 1, so h(x, y) = x + y - 1.', 'Lagrangian: L = x^2 + y^2 + lambda(x + y - 1). Setting its derivatives to zero: 2x + lambda = 0, 2y + lambda = 0, and x + y = 1.', 'The first two give x = y = -lambda/2; the constraint then gives x = y = 1/2 and lambda = -1. The minimum value is f = 1/2.', 'Tangency check: grad f = [2x, 2y] = [1, 1] and grad h = [1, 1] are parallel, and grad f + lambda grad h = 0.', 'Inequality version: minimize (x - 2)^2 subject to x <= 1, so g(x) = x - 1. The unconstrained minimum x = 2 is not allowed, so the constraint is active at x = 1, and 2(x - 2) + lambda = 0 gives lambda = 2 >= 0. Loosening the bound to x <= 1 + e lowers the best value by about 2e: lambda measures the price of the constraint.'],
    graph: { type: 'lagrange', title: 'Tangency at the constrained minimum', caption: 'Slide the point along the constraint x + y = c. The circle is the level curve of f = x^2 + y^2 through it. At the minimum the circle just touches the line and the two gradients are parallel.', sliders: [{ key: 's', label: 'position along the line', min: -3, max: 3, step: 0.1, value: 1.5 }, { key: 'c', label: 'constraint level c', min: 0.5, max: 4, step: 0.5, value: 2 }] },
    figure: 'lagrange-tangent',
    misconception: 'At a constrained minimum the gradient of f is usually not zero; only its component along the constraint vanishes. Setting grad f = 0 and ignoring the constraint finds the unconstrained minimum, which may break the constraint.',
    prerequisites: ['partial-derivatives-gradient', 'convex-functions'],
    followOns: ['ridge-regularization', 'lasso'],
    sources: ['Mathematics for Machine Learning, §7.2'],
  },
];
